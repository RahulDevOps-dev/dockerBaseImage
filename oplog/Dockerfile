# Use Ubuntu as the base image
FROM ubuntu:20.04

# Set the working directory inside the container
WORKDIR /app

# Set a default command to run when the container starts
CMD ["echo", "Hello from Ubuntu Minimal!"]

